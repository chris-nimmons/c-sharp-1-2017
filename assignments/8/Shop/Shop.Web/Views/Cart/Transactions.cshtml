@model Transaction
@{
    Layout = null;
}

<div class="row">
    <a href="~/cart/transactions" class="btn btn-success pull-left">Transactions</a>

    <table class="table">
        @{ decimal subTotal = 0; }
        @foreach (var order in Model.Orders)
        {
            subTotal += order.Quantity * order.Product.Price;
            <tr>
                <td></td>
                <td>
                    @order.Product.Name
                </td>
                <td>
                    @order.Quantity + @order.Product.Price
                </td>
                <td>
                    @subTotal
                </td>
                <td>
                    <a href="~/cart/remove/@order.Product.Id" class="btn btn-danger btn-block">Remove</a>
                </td>
            </tr>
        }
        <tr>
            <td><strong>Transactions</strong></td>
            <td colspan="3"></td>
            <td> @Model.Orders</td>
        </tr>
    </table>
    <a href="~/cart/transactions" class="btn btn-primary">Transaction History</a>
    k
</div>
